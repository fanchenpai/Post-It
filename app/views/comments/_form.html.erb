<h4>Comments</h4>

<%= render 'shared/form_error', object: @comment %>

<%= form_for [@post, @comment] do |f| %>
  <%= f.label :body, "Leave a comment" %>
  <%= f.text_area :body %>
  <br>
  <%= f.submit "Create", class: "btn" %>
<% end %>