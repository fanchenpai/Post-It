<%= render 'shared/form_error', object: @category %>

<%= form_for @category do |f| %>
  <%= f.label :name, "Name" %>
  <%= f.text_field :name %>
  <br>
  <%= f.submit "Save", class: "btn" %>
<% end %>